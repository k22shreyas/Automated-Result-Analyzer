RUN app.py TO START THE SERVER THEN OPEN THE SHOWN HOST URL TO GET USER INTERFACE

Make sure applications are installed in the specified locations...

1. Download and install python 3.10.0 from

Link -> https://www.python.org/downloads/release/python-3100/
Set env_path for python from 
start->edit system environment variable->environment variables->select and edit {path},in system variables->add C:\Python310 or the appropriate path for your version of python 

2. Check Google Chrome version installed in your PC and downlaod the {ChromeDriver} of the same version.

Link -> https://chromedriver.chromium.org/downloads
Copy chromedriver.exe to the installer directory.

					OR

Installe Chrome version102 present in the installer directory and 
leave the chromedriver version102 which is already in the directory as it is.


3. Tesseract OCR: Install the setup present in the directory and set path as specfied below.
C:\Program Files\Tesseract-OCR\

					OR

Download and install newer version of tesseract {choose 64-bit} from 
Link -> https://github.com/UB-Mannheim/tesseract/wiki 

